# Application assets

`photocrop.ico` is the shared Windows icon for the Qt application, the PyInstaller executable, and the Inno Setup installer. Regenerate it with:

```powershell
.\.venv\Scripts\python.exe tools\build_icon.py
```
